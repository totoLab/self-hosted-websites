---
const { title } = Astro.props;
---
<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{title}</title>
    <style>
        /* Base styles with a dark theme suitable for programmers */
        :root {
            --bg-primary: #1a1a1a;
            --text-primary: #e0e0e0;
            --accent: #64ffda;
        }

        body {
            background-color: var(--bg-primary);
            color: var(--text-primary);
            font-family: 'JetBrains Mono', monospace;
            margin: 0;
            padding: 0;
            line-height: 1.6;
        }

        .container {
            max-width: 1200px;
            margin: 0 auto;
            padding: 0 1rem;
        }

        header {
            padding: 1rem 0;
            border-bottom: 1px solid var(--accent);
            display: flex;
            align-items: center;
            gap: 0.5rem;
        }

        header img.logo {
            width: 2rem;
            height: auto;
            filter: invert(1) brightness(1000%); /* Inverts and brightens the logo */
        }

        h1 {
            font-size: 1.5rem;
        }
        
        nav {
            padding: 1rem 0;
            display: flex;
            gap: 1rem;
        }

        a {
            color: var(--accent);
            text-decoration: none;
        }

        a:hover {
            text-decoration: underline;
        }

        footer {
            margin-top: 2rem;
            padding: 1rem 0;
            border-top: 1px solid var(--accent);
            text-align: center;
        }
    </style>
</head>
<body>
    <header>
        <div class="container">
            <div class="container">
                <img class="logo" src="/logo.svg" alt="Logo">
                <h1><a class="title" href="/">antolab.xyz</a></h1>
            </div>
        </div>
    </header>
    <nav class="container">
        <a href="/project">Projects</a>
        <a href="/sites">Sites</a>
        <a href="https://www.linkedin.com/in/antoniolabate/">Linkedin</a>
        <a href="https://github.com/totoLab/" target="_blank">GitHub</a>
        <a href="/contact">Contact</a>
    </nav>
    <main class="container">
        <slot />
    </main>
    <footer>
        <p>&copy; {new Date().getFullYear()} antolab.xyz. Built with Astro 🚀</p>
    </footer>
</body>
</html>
